# Mango Cinema — Demo Web

Deploy via Vercel: import this git repo, set **Root Directory** to `apps/web`, choose the Next.js preset (auto-detected), leave Install Command as `pnpm install` (detected from lockfile), and click Deploy.
